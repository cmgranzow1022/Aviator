@model IEnumerable<Aviator.Models.Plane>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TailNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalEngineHours)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalHobbsHours)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TailNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalEngineHours)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalHobbsHours)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PlaneId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlaneId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlaneId })
        </td>
    </tr>
}

</table>
